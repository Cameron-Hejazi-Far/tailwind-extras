module.exports = ({addUtilities, addComponents, addVariant, e, prefix, config}) => {

    // addComponents({
    // TODO: implement
    // });

};

/*
.table {
  border-collapse: collapse;
  -webkit-appearance: none;
     -moz-appearance: none;
          appearance: none;
  color: #3d4852;
  background-color: #fff;
  width: 100%;
  font-size: .875rem;
  border-spacing: 0;
}

.table thead,
.table tbody,
.table tfoot {
  background-color: transparent;
}

.table th {
  font-weight: 700;
}

.table thead th,
.table thead td {
  vertical-align: middle;
  text-align: left;
  padding-left: .75rem;
  padding-right: .75rem;
  padding-top: .5rem;
  padding-bottom: .5rem;
  border-width: 0;
  border-bottom-width: 1px;
  border-color: #b8c2cc;
}

.table tbody th,
.table tbody td {
  vertical-align: middle;
  text-align: left;
  padding-left: .75rem;
  padding-right: .75rem;
  padding-top: .5rem;
  padding-bottom: .5rem;
  border-width: 0;
  border-top-width: 1px;
  border-color: #b8c2cc;
}

.table tbody tr:first-child td,
.table tbody tr:first-child th {
  border-top-width: 0;
  border-color: #b8c2cc;
}

.table tfoot th,
.table tfoot td {
  vertical-align: middle;
  text-align: left;
  padding-left: .75rem;
  padding-right: .75rem;
  padding-top: .5rem;
  padding-bottom: .5rem;
  border-width: 0;
  border-top-width: 1px;
  border-color: #b8c2cc;
}

.table.narrow th,
.table.narrow td {
  padding-left: .5rem;
  padding-right: .5rem;
  padding-top: .25rem;
  padding-bottom: .25rem;
}

.table.bordered {
  border-width: 1px;
  border-color: #b8c2cc;
}

.table.bordered th,
.table.bordered td {
  border-color: #b8c2cc;
}

.table.striped tbody tr:nth-child(even) td {
  background-color: #eff8ff;
}

.table.hoverable tbody tr:hover td {
  background-color: #eff8ff;
}

.table.hoverable.striped tbody tr:nth-child(even):hover td {
  background-color: #bcdefa;
}











.table {
  @apply border-collapse appearance-none text-grey-darkest bg-white w-full text-sm;
  border-spacing: 0;

  & thead, & tbody, & tfoot {
    @apply bg-transparent;
  }

  // TH Cells
  & th {
    @apply font-bold;
  }

  // All Header Cells
  & thead th, & thead td {
    @apply align-middle text-left px-3 py-2 border-0 border-b border-grey;
  }

  // All Body Cells
  & tbody th, & tbody td {
    @apply align-middle text-left px-3 py-2 border-0 border-t border-grey;
  }
  & tbody tr:first-child td, & tbody tr:first-child th {
    @apply border-t-0 border-grey;
  }

  // All Footer Cells
  & tfoot th, & tfoot td {
    @apply align-middle text-left px-3 py-2 border-0 border-t border-grey;
  }

  // Narrow Table
  &.narrow th, &.narrow td {
    @apply px-2 py-1;
  }

  // Bordered Table
  &.bordered {
    @apply border border-grey;
    & th, & td {
      @apply border-grey;
    }
  }

  // Striped Table
  &.striped tbody tr:nth-child(even) td {
    @apply bg-blue-lightest;
  }

  // Hoverable Table
  &.hoverable tbody tr:hover td {
    @apply bg-blue-lightest;
  }
  &.hoverable.striped tbody tr:nth-child(even):hover td {
    @apply bg-blue-lighter;
  }
}

 */